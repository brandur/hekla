#!/usr/bin/env ruby

require "bundler/setup"
Bundler.require

require "irb"

$: << "./lib"
require "hekla"

require_relative "../models/article"

DB = Sequel.connect(Hekla::Config.database_url)

IRB.start
