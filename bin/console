#!/usr/bin/env ruby

require "bundler/setup"
Bundler.require

require "irb"

$: << "./lib"
require "hekla"

DB = Sequel.connect(Hekla::Config.database_url)

require_relative "../models/article"

IRB.start
